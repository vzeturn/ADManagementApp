<Application x:Class="ADManagementApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:helpers="clr-namespace:ADManagementApp.Helpers"
             xmlns:viewModels="clr-namespace:ADManagementApp.ViewModels"
             xmlns:views="clr-namespace:ADManagementApp.Views">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Material Design Themes -->
                <materialDesign:BundledTheme BaseTheme="Light" 
                                           PrimaryColor="Blue" 
                                           SecondaryColor="Amber" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- DataTemplates for MVVM - Maps ViewModels to Views -->
            <DataTemplate DataType="{x:Type viewModels:DashboardViewModel}">
                <views:DashboardView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewModels:UserManagementViewModel}">
                <views:UserManagementView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewModels:GroupManagementViewModel}">
                <views:GroupManagementView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewModels:SettingsViewModel}">
                <views:SettingsView />
            </DataTemplate>

            <!-- Converters -->
            <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <helpers:BoolToColorConverter x:Key="BoolToColorConverter"/>
            <helpers:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
            <helpers:InverseBoolConverter x:Key="InverseBoolConverter"/>
            <helpers:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
            <helpers:DateTimeToStringConverter x:Key="DateTimeToStringConverter"/>
            <helpers:UserStatusToIconConverter x:Key="UserStatusToIconConverter"/>
            <helpers:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
            <helpers:PercentageConverter x:Key="PercentageConverter"/>

            <!-- Styles -->
            <Style x:Key="PageTitle" TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignHeadline4TextBlock}">
                <Setter Property="Margin" Value="0,0,0,20"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </Style>

            <Style x:Key="CardStyle" TargetType="materialDesign:Card">
                <Setter Property="Padding" Value="20"/>
                <Setter Property="Margin" Value="0,0,0,15"/>
                <Setter Property="UniformCornerRadius" Value="8"/>
            </Style>

            <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
                <Setter Property="Margin" Value="5,0"/>
                <Setter Property="Width" Value="36"/>
                <Setter Property="Height" Value="36"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
