<Window x:Class="ADManagementApp.Views.CreateGroupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Create New Group" 
        Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="AccountMultiplePlus" Width="32" Height="32" 
                                        VerticalAlignment="Center" Margin="0,0,10,0"
                                        Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                <TextBlock Text="Create New Group" 
                          Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                          FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="Enter the details for the new Active Directory group" 
                      Style="{StaticResource MaterialDesignBody2TextBlock}"
                      Foreground="{DynamicResource MaterialDesignBodyLight}"
                      Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Form Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <materialDesign:Card Padding="20" UniformCornerRadius="8">
                    <StackPanel>
                        <!-- Group Name -->
                        <TextBox x:Name="GroupNameTextBox"
                                materialDesign:HintAssist.Hint="Group Name *"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Margin="0,0,0,15"
                                MaxLength="64"/>

                        <!-- SAM Account Name -->
                        <TextBox x:Name="SamAccountNameTextBox"
                                materialDesign:HintAssist.Hint="SAM Account Name *"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Margin="0,0,0,15"
                                MaxLength="20"
                                materialDesign:HintAssist.HelperText="Pre-Windows 2000 group name (max 20 characters)"/>

                        <!-- Description -->
                        <TextBox x:Name="DescriptionTextBox"
                                materialDesign:HintAssist.Hint="Description"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                AcceptsReturn="True"
                                TextWrapping="Wrap"
                                VerticalScrollBarVisibility="Auto"
                                MinLines="3"
                                MaxLines="5"
                                Margin="0,0,0,15"
                                MaxLength="1024"/>

                        <!-- Group Scope -->
                        <TextBlock Text="Group Scope" 
                                  Style="{StaticResource MaterialDesignBody2TextBlock}"
                                  Margin="0,0,0,5"/>
                        <ComboBox x:Name="GroupScopeComboBox"
                                 Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                 Margin="0,0,0,15"
                                 SelectedIndex="0">
                            <ComboBoxItem Content="Global"/>
                            <ComboBoxItem Content="Universal"/>
                            <ComboBoxItem Content="DomainLocal"/>
                        </ComboBox>

                        <!-- Group Type -->
                        <TextBlock Text="Group Type" 
                                  Style="{StaticResource MaterialDesignBody2TextBlock}"
                                  Margin="0,0,0,5"/>
                        <ComboBox x:Name="GroupTypeComboBox"
                                 Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                 Margin="0,0,0,15"
                                 SelectedIndex="0">
                            <ComboBoxItem Content="Security"/>
                            <ComboBoxItem Content="Distribution"/>
                        </ComboBox>

                        <!-- Required Fields Note -->
                        <Border Background="{DynamicResource MaterialDesignToolBarBackground}"
                               Padding="10"
                               CornerRadius="4"
                               Margin="0,10,0,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Information" 
                                                        Width="16" Height="16"
                                                        VerticalAlignment="Center"
                                                        Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                <TextBlock Text="* Required fields" 
                                          Margin="10,0,0,0"
                                          VerticalAlignment="Center"
                                          Style="{StaticResource MaterialDesignBody2TextBlock}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                   HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Style="{StaticResource MaterialDesignFlatButton}"
                   Content="Cancel"
                   x:Name="CancelButton"
                   Click="CancelButton_Click"
                   Margin="0,0,10,0"/>

            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                   Content="Create Group"
                   x:Name="CreateButton"
                   Click="CreateButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
