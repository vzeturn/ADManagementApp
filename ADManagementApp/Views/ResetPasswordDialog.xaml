<Window x:Class="ADManagementApp.Views.ResetPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Reset Password" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Reset Password" 
                  Style="{StaticResource MaterialDesignHeadline5TextBlock}" 
                  Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <PasswordBox x:Name="PasswordBox"
                        materialDesign:HintAssist.Hint="New Password"
                        Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                        Margin="0,0,0,15"/>
            <PasswordBox x:Name="ConfirmPasswordBox"
                        materialDesign:HintAssist.Hint="Confirm Password"
                        Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                        Margin="0,0,0,15"/>
            <CheckBox x:Name="MustChangeCheckBox"
                     Content="User must change password at next logon"
                     IsChecked="True"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Click="CancelButton_Click" Margin="0,0,10,0"/>
            <Button Content="Reset" Click="ResetButton_Click" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>