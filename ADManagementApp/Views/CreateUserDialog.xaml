<Window x:Class="ADManagementApp.Views.CreateUserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Create New User" 
        Height="600" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="AccountPlus" Width="32" Height="32" 
                                        VerticalAlignment="Center" Margin="0,0,10,0"
                                        Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                <TextBlock Text="Create New User" 
                          Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                          FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="Enter user details for Active Directory" 
                      Style="{StaticResource MaterialDesignBody2TextBlock}"
                      Foreground="{DynamicResource MaterialDesignBodyLight}"
                      Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Form Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <materialDesign:Card Padding="20" UniformCornerRadius="8">
                    <StackPanel>
                        <!-- Username -->
                        <TextBox x:Name="UsernameTextBox"
                                materialDesign:HintAssist.Hint="Username (SAM Account Name) *"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Margin="0,0,0,15"
                                MaxLength="20"/>

                        <!-- First Name -->
                        <TextBox x:Name="FirstNameTextBox"
                                materialDesign:HintAssist.Hint="First Name *"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Margin="0,0,0,15"/>

                        <!-- Last Name -->
                        <TextBox x:Name="LastNameTextBox"
                                materialDesign:HintAssist.Hint="Last Name *"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Margin="0,0,0,15"/>

                        <!-- Display Name -->
                        <TextBox x:Name="DisplayNameTextBox"
                                materialDesign:HintAssist.Hint="Display Name *"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Margin="0,0,0,15"
                                LostFocus="DisplayNameTextBox_LostFocus"/>

                        <!-- Email -->
                        <TextBox x:Name="EmailTextBox"
                                materialDesign:HintAssist.Hint="Email Address"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Margin="0,0,0,15"/>

                        <!-- Password -->
                        <PasswordBox x:Name="PasswordBox"
                                    materialDesign:HintAssist.Hint="Password *"
                                    Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                                    Margin="0,0,0,15"/>

                        <!-- Confirm Password -->
                        <PasswordBox x:Name="ConfirmPasswordBox"
                                    materialDesign:HintAssist.Hint="Confirm Password *"
                                    Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                                    Margin="0,0,0,15"/>

                        <!-- Department -->
                        <TextBox x:Name="DepartmentTextBox"
                                materialDesign:HintAssist.Hint="Department"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Margin="0,0,0,15"/>

                        <!-- Job Title -->
                        <TextBox x:Name="TitleTextBox"
                                materialDesign:HintAssist.Hint="Job Title"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Margin="0,0,0,15"/>

                        <!-- Phone -->
                        <TextBox x:Name="PhoneTextBox"
                                materialDesign:HintAssist.Hint="Phone Number"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Margin="0,0,0,15"/>

                        <!-- Description -->
                        <TextBox x:Name="DescriptionTextBox"
                                materialDesign:HintAssist.Hint="Description"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                AcceptsReturn="True"
                                TextWrapping="Wrap"
                                MinLines="2"
                                MaxLines="4"
                                Margin="0,0,0,15"/>

                        <!-- User Options -->
                        <StackPanel>
                            <CheckBox x:Name="EnabledCheckBox"
                                     Content="User Enabled"
                                     IsChecked="True"
                                     Margin="0,0,0,10"/>

                            <CheckBox x:Name="MustChangePasswordCheckBox"
                                     Content="User must change password at next logon"
                                     IsChecked="True"
                                     Margin="0,0,0,10"/>

                            <CheckBox x:Name="PasswordNeverExpiresCheckBox"
                                     Content="Password never expires"
                                     IsChecked="False"
                                     Margin="0,0,0,10"/>
                        </StackPanel>

                        <!-- Required Fields Note -->
                        <Border Background="{DynamicResource MaterialDesignToolBarBackground}"
                               Padding="10"
                               CornerRadius="4"
                               Margin="0,10,0,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Information" 
                                                        Width="16" Height="16"
                                                        VerticalAlignment="Center"
                                                        Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                <TextBlock Text="* Required fields" 
                                          Margin="10,0,0,0"
                                          VerticalAlignment="Center"
                                          Style="{StaticResource MaterialDesignBody2TextBlock}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                   HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Style="{StaticResource MaterialDesignFlatButton}"
                   Content="Cancel"
                   x:Name="CancelButton"
                   Click="CancelButton_Click"
                   Margin="0,0,10,0"/>

            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                   Content="Create User"
                   x:Name="CreateButton"
                   Click="CreateButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
