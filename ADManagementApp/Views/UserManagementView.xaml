<UserControl x:Class="ADManagementApp.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="User Management" 
                      Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                      FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="Manage Active Directory users" 
                      Style="{StaticResource MaterialDesignBody2TextBlock}"
                      Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </StackPanel>

        <!-- Toolbar -->
        <materialDesign:Card Grid.Row="1" Padding="15" Margin="0,0,0,15" UniformCornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <TextBox Grid.Column="0" 
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="Search users (username, name, email)..."
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        VerticalAlignment="Center"
                        Margin="0,0,10,0">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Style="{StaticResource MaterialDesignIconButton}"
                           Command="{Binding SearchCommand}"
                           ToolTip="Search">
                        <materialDesign:PackIcon Kind="Magnify"/>
                    </Button>

                    <Button Style="{StaticResource MaterialDesignIconButton}"
                           Command="{Binding LoadUsersCommand}"
                           ToolTip="Refresh">
                        <materialDesign:PackIcon Kind="Refresh"/>
                    </Button>

                    <!--<Separator Style="{StaticResource MaterialDesignVerticalSeparator}" Margin="5,0"/>-->

                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                           Command="{Binding CreateUserCommand}"
                           Margin="5,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="AccountPlus" VerticalAlignment="Center"/>
                            <TextBlock Text="Create User" Margin="10,0,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Users DataGrid -->
        <materialDesign:Card Grid.Row="2" UniformCornerRadius="8">
            <Grid>
                <!-- Loading Overlay -->
                <Border Background="#80000000" 
                       Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                       Panel.ZIndex="999">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                    Value="0" IsIndeterminate="True"
                                    Width="60" Height="60"
                                    Foreground="White"/>
                        <TextBlock Text="Loading..." Foreground="White" 
                                  Margin="0,10,0,0" FontSize="16"/>
                    </StackPanel>
                </Border>

                <!-- DataGrid -->
                <DataGrid ItemsSource="{Binding Users}"
                         SelectedItem="{Binding SelectedUser}"
                         AutoGenerateColumns="False"
                         CanUserAddRows="False"
                         CanUserDeleteRows="False"
                         SelectionMode="Single"
                         IsReadOnly="True"
                         materialDesign:DataGridAssist.CellPadding="10"
                         materialDesign:DataGridAssist.ColumnHeaderPadding="10">

                    <DataGrid.Columns>
                        <!-- Username -->
                        <DataGridTextColumn Header="Username" 
                                          Binding="{Binding SamAccountName}"
                                          Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Display Name -->
                        <DataGridTextColumn Header="Display Name" 
                                          Binding="{Binding DisplayName}"
                                          Width="200"/>

                        <!-- Email -->
                        <DataGridTextColumn Header="Email" 
                                          Binding="{Binding EmailAddress}"
                                          Width="200"/>

                        <!-- Status -->
                        <DataGridTemplateColumn Header="Status" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="12" Height="12" VerticalAlignment="Center">
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Setter Property="Fill" Value="#F44336"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Enabled}" Value="True">
                                                            <Setter Property="Fill" Value="#4CAF50"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>
                                        <TextBlock Text="{Binding StatusText}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Last Logon -->
                        <DataGridTextColumn Header="Last Logon" 
                                          Binding="{Binding LastLogonText}"
                                          Width="150"/>

                        <!-- Groups -->
                        <DataGridTextColumn Header="Groups" 
                                          Binding="{Binding GroupsText}"
                                          Width="*"/>

                        <!-- Actions -->
                        <DataGridTemplateColumn Header="Actions" Width="280">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <!-- View Details Button -->
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                               ToolTip="View Details"
                                               Width="32" Height="32"
                                               Margin="2,0"
                                               Command="{Binding DataContext.ViewDetailsCommand, 
                                                       RelativeSource={RelativeSource AncestorType=UserControl}}">
                                            <materialDesign:PackIcon Kind="Eye" Width="18" Height="18"/>
                                        </Button>

                                        <!-- Edit Button -->
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                               ToolTip="Edit"
                                               Width="32" Height="32"
                                               Margin="2,0"
                                               Command="{Binding DataContext.EditUserCommand, 
                                                       RelativeSource={RelativeSource AncestorType=UserControl}}">
                                            <materialDesign:PackIcon Kind="Pencil" Width="18" Height="18"/>
                                        </Button>

                                        <!-- Reset Password Button -->
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                               ToolTip="Reset Password"
                                               Width="32" Height="32"
                                               Margin="2,0"
                                               Command="{Binding DataContext.ResetPasswordCommand, 
                                                       RelativeSource={RelativeSource AncestorType=UserControl}}">
                                            <materialDesign:PackIcon Kind="Key" Width="18" Height="18"/>
                                        </Button>

                                        <!-- Enable/Disable Button -->
                                        <Button Width="32" Height="32" Margin="2,0">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
                                                    <Setter Property="ToolTip" Value="Disable User"/>
                                                    <Setter Property="Command" 
                                                           Value="{Binding DataContext.DisableUserCommand, 
                                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Enabled}" Value="False">
                                                            <Setter Property="ToolTip" Value="Enable User"/>
                                                            <Setter Property="Command" 
                                                                   Value="{Binding DataContext.EnableUserCommand, 
                                                                          RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                            <materialDesign:PackIcon Width="18" Height="18">
                                                <materialDesign:PackIcon.Style>
                                                    <Style TargetType="materialDesign:PackIcon">
                                                        <Setter Property="Kind" Value="Lock"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Enabled}" Value="False">
                                                                <Setter Property="Kind" Value="LockOpen"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </materialDesign:PackIcon.Style>
                                            </materialDesign:PackIcon>
                                        </Button>

                                        <!-- Unlock Account Button -->
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                               ToolTip="Unlock Account"
                                               Width="32" Height="32"
                                               Margin="2,0"
                                               Command="{Binding DataContext.UnlockAccountCommand, 
                                                       RelativeSource={RelativeSource AncestorType=UserControl}}">
                                            <materialDesign:PackIcon Kind="LockOpenVariant" Width="18" Height="18"/>
                                        </Button>

                                        <!-- Delete Button -->
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                               ToolTip="Delete"
                                               Width="32" Height="32"
                                               Margin="2,0"
                                               Foreground="#F44336"
                                               Command="{Binding DataContext.DeleteUserCommand, 
                                                       RelativeSource={RelativeSource AncestorType=UserControl}}">
                                            <materialDesign:PackIcon Kind="Delete" Width="18" Height="18"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
