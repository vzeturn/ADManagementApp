<Window x:Class="ADManagementApp.Views.EditUserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Edit User" Height="500" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Edit User" 
                  Style="{StaticResource MaterialDesignHeadline5TextBlock}" 
                  Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <TextBox x:Name="DisplayNameTextBox"
                        materialDesign:HintAssist.Hint="Display Name"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,15"/>
                <TextBox x:Name="FirstNameTextBox"
                        materialDesign:HintAssist.Hint="First Name"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,15"/>
                <TextBox x:Name="LastNameTextBox"
                        materialDesign:HintAssist.Hint="Last Name"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,15"/>
                <TextBox x:Name="EmailTextBox"
                        materialDesign:HintAssist.Hint="Email"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,15"/>
                <TextBox x:Name="DepartmentTextBox"
                        materialDesign:HintAssist.Hint="Department"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,15"/>
                <TextBox x:Name="TitleTextBox"
                        materialDesign:HintAssist.Hint="Title"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,15"/>
                <TextBox x:Name="PhoneTextBox"
                        materialDesign:HintAssist.Hint="Phone"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,15"/>
                <TextBox x:Name="DescriptionTextBox"
                        materialDesign:HintAssist.Hint="Description"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        MinLines="2"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Click="CancelButton_Click" Margin="0,0,10,0"/>
            <Button Content="Save" Click="SaveButton_Click" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>